# Windows build/runtime dependencies for LabelmeAI installer (packaging/windows)
# Recommended Python: 3.11 x64

# Build
pyinstaller==6.3.0

# Fix: PyInstaller rthook `pyi_rth_pkgres` imports pkg_resources, which requires jaraco.*
jaraco.text==3.12.1
jaraco.functools==4.0.1
jaraco.context==5.3.0
jaraco.collections==5.1.0

# Geometry (required by labelme.dlcv.*)
shapely==2.0.3

# GPU driver query (labelme imports `pynvml`; provided by nvidia-ml-py)
nvidia-ml-py==13.590.44

# Qt (repo contains direct PyQt5 imports)
PyQt5==5.15.10
qtpy==2.4.1
qasync==0.27.1

# Core (pin numpy<2 for onnxruntime compatibility; pin opencv accordingly)
numpy==1.26.4
opencv-python==4.10.0.84
imgviz
scikit-image
PyYAML
natsort
Pillow
termcolor
ordered_set
openpyxl
pyqtgraph

# Networking
aiohttp
httpx

# AI runtime (pinned to avoid DLL init failure)
onnxruntime==1.17.3
gdown
osam

# Optional features
open3d
pyqt-toast-notification

